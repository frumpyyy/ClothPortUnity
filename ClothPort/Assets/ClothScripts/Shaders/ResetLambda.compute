#pragma kernel ResetKernel

#include "ClothCommon.hlsl"

RWStructuredBuffer<Spring> Springs;

uint numSprings;

[numthreads(256, 1, 1)]
void ResetKernel(uint id : SV_DispatchThreadID)
{
    if (id >= numSprings)
        return;

    Springs[id].lambda = 0.0f;
}